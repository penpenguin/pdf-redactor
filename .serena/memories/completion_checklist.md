# When a task is completed
- Run `npm test`.
- Run `npm run build`.
- Mention any remaining build/runtime warnings, especially bundle size or browser-compatibility warnings from Vite.
- If deployment-related files changed, confirm GitHub Pages compatibility (relative `base`, static assets in `dist/`).