# Suggested commands
- `npm install` — install dependencies
- `npm run dev` — start local Vite dev server
- `npm test` — run Vitest suite
- `npm run build` — create static production build in `dist/`
- `git status --short` — inspect worktree changes
- `find . -maxdepth 2 -type f | sort` — quick file inventory